(mdx
 (libraries smtml smtml.prelude zarith fmt)
 (enabled_if %{lib-available:z3}))

(env
 (release
  (ocamlopt_flags
   (:standard -O3)))
 (benchmark
  (ocamlopt_flags
   (:standard -O3 -unsafe -noassert))))
