(ocamllex
 (modules lexer))

(menhir
 (modules parser))

(library
 (public_name encoding)
 (name encoding)
 (modules
  ast
  ;axioms
  eval_numeric
  expr
  interpret
  interpret_intf
  lexer
  log
  mappings_intf
  model
  num
  op_intf
  optimizer
  parser
  params
  parse
  solver
  solver_intf
  symbol
  ty
  value
  z3_mappings)
 (private_modules lexer parser)
 (libraries z3))
