(ocamllex
 (modules lexer))

(menhir
 (modules parser))

(library
 (public_name encoding)
 (name encoding)
 (modules
  ast
  ;axioms
  batch
  bitv
  ;boolean
  eval_numeric
  expr
  ;expression
  ;floatingPoint
  incremental
  ;integer
  interpret
  interpret_intf
  lexer
  mappings_intf
  model
  num
  op_intf
  optimizer
  parser
  params
  ;real
  run
  solver_intf
  ;strings
  symbol
  ty
  value
  z3_mappings)
 (libraries z3))
