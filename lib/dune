(ocamllex
 (modules lexer))

(menhir
 (modules parser))

(library
 (public_name encoding)
 (name encoding)
 (modules
  ast
  ;axioms
  batch
  eval_numeric
  expr
  incremental
  interpret
  interpret_intf
  lexer
  mappings_intf
  model
  num
  op_intf
  optimizer
  parser
  params
  run
  solver_intf
  symbol
  ty
  value
  z3_mappings)
 (libraries z3))
