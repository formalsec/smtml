(ocamllex
 (modules lexer smtlib_lexer))

(menhir
 (modules parser smtlib_parser))

(library
 (public_name encoding)
 (name encoding)
 (modules
  ast
  ;axioms
  eval_numeric
  expr
  interpret
  interpret_smt
  interpret_intf
  lexer
  mappings_intf
  model
  num
  op_intf
  optimizer
  parse
  parser
  params
  smtlib
  smtlib_lexer
  smtlib_parser
  solver
  solver_intf
  symbol
  syntax
  ty
  value
  z3_mappings)
 (private_modules syntax)
 (libraries z3 bitv menhirLib))
